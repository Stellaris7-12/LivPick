好的，我们开始。既然你说项目经历了“全方位拷打”，那我就不客气了。我会从**架构设计、技术细节、异常边界、性能压测、数据一致性**五个维度，逐层深挖你的第一个秒杀项目。请做好准备。

---

### 第一轮：整体架构与核心链路

**面试官**：先介绍一下你的秒杀核心链路吧。从用户点击“立即抢购”到最终订单生成（或失败返回），请求都经过了哪些组件？用了什么同步/异步策略？

追问：
1. 你说“5k总并发，3.4w总请求”，这个数据是怎么测出来的？并发模型是怎样的（如梯度加压、思考时间）？5000并发下QPS只有500+，为什么这么低？是Redis单节点瓶颈还是业务逻辑限制？
2. “最终仅500个请求进入成功链路” —— 这500个是秒杀成功（获得下单资格）的数量吗？库存总量是多少？如果库存是500，那是否意味着所有成功请求都能最终下单？有没有可能因为异步落库失败导致成功数少于库存？

---

### 第二轮：库存预扣与防超卖（Lua + Redis）

**面试官**：你说用“Redis+Lua原子化资格校验，同时完成库存预扣”。请把Lua脚本的核心逻辑（伪代码或关键步骤）写出来。需要包含：用户资格校验（是否已抢到/是否已下单）、库存扣减、记录抢购成功标识。

追问：
1. 这个脚本里如何判断用户“已经抢到过”？是用的Redis的Set还是String？如果用户在一秒内重复发起请求（防重放），你的Lua脚本能防住吗？还是依赖前端或网关限流？
2. 假设库存为1，两个请求同时到达Lua脚本执行，Redis单线程原子性可以保证不会超卖。但**库存扣减成功后，后续异步落库如果失败（比如Kafka宕机）**，你怎么办？会不会出现“库存已扣但订单未生成”的库存黑洞？
3. 你的Lua脚本里是否包含了“检查用户是否在等待支付队列中”的逻辑？因为你说自动关单后恢复资格，那么关单恢复资格时修改了哪个数据结构？Lua脚本资格校验时能感知到吗？

---

### 第三轮：Kafka异步落库与消息可靠性

**面试官**：引入Kafka是为了削峰填谷。请描述：成功预扣库存后，你往Kafka发送的消息体包含哪些字段？Consumer如何消费并生成订单？消费者失败后如何处理？

追问：
1. **消息丢失风险**：Producer发送消息时是`acks=all`吗？你如何确保消息一定到达Broker？如果发送失败（比如网络抖动），你是重试还是丢弃？对用户而言，此时用户已经看到“抢购成功”了吗？
2. **消息重复消费**：Kafka的Consumer在处理订单时，如果订单已插入但后续提交offset前进程崩溃，重启后会重复消费。你如何保证**幂等**？数据库唯一键是订单号还是秒杀活动ID+用户ID？
3. **消息积压**：假设瞬间500个成功请求全进入Kafka，但你的Consumer实例只有1个，下单涉及MySQL插入优惠券、订单、扣减真实库存等操作，TPS可能只有100。消息积压越来越严重，用户迟迟收不到订单结果，你怎么处理？会触发什么监控和熔断？

---

### 第四轮：自动关单与库存恢复（Redisson + Spring Task）

**面试官**：你提到“基于Redisson延迟队列 + Spring Task兜底实现自动关单，关单后自动恢复库存及购买资格”。详细解释：
- Redisson的`RDelayedQueue`底层依赖Redis的ZSet，你是把订单ID作为value，关单时间戳作为score吗？
- 关单时，如何判断订单确实未支付？是否存在“用户刚好在第29分59秒支付，关单任务在第30分00秒执行”的竞态条件？你用了分布式锁吗？

追问：
1. 你压测得到“P95延迟从4055ms降至533ms”，这个P95是**从创建订单到关单任务触发的端到端延迟**吗？对比的是什么方案？如果是Spring Task轮询数据库，为什么之前那么慢？
2. **关单恢复库存**：恢复时是否重新执行Lua脚本中的资格校验？假设库存恢复后，原订单用户再次抢购，你的Lua如何知道该用户“曾经抢到过但订单已关闭，允许再次抢购”？这个状态机你是如何维护的？
3. 如果Redisson的延迟队列因为Redis宕机而丢失消息，Spring Task兜底是怎么做的？全表扫描`status=待支付 and create_time < now - 30min`？这样在高并发下会不会造成数据库压力？

---

### 第五轮：缓存异常防护（布隆过滤器、缓存击穿、一致性）

**面试官**：你说“纯非法请求场景下，数据库回源次数从26次降至0”。非法请求是指什么？布隆过滤器拦截的是**不存在的商品ID**吗？布隆过滤器如何预热？如果商品ID是数字自增的，攻击者用1到1000000遍历，布隆过滤器能拦截多少？

追问：
1. **缓存击穿**：你说用“逻辑过期+异步刷新”。请描述具体实现：缓存中存储的逻辑过期时间字段怎么判断？当多个线程同时发现一个热点Key逻辑过期，你怎么保证只有一个线程去加载DB？其他线程是等待还是返回旧数据？
2. **缓存与数据库最终一致性**：你提到“旁路缓存 + MQ补偿重试 + TTL兜底”。当运营后台修改优惠券库存时，你是先更新DB还是先删缓存？如果采用先删缓存，在“删缓→写DB”之间，有读请求将旧数据再次加载到缓存，怎么解决？
3. **缓存空值**：你说用了“缓存空值”，那空值缓存过期时间设了多久？如果恶意用户请求大量不存在的商品ID，你会在Redis里写入大量空值，导致内存膨胀吗？有什么防范措施？

---

### 第六轮：压测与监控

**面试官**：你说做过压测，用的是JMeter还是其他工具？压测时关注了哪些指标？有没有发现系统的最短板在哪里？

追问：
1. **Redis瓶颈**：5000并发下，你的Redis节点CPU使用率多少？Lua脚本执行的平均耗时是多少？有没有开启Pipeline或连接池优化？
2. **Kafka瓶颈**：成功请求只有500个，Kafka几乎无压力。如果库存扩大到5000，成功请求5000，Kafka生产者会不会成为瓶颈？你做过更大规模的压测吗？
3. **数据库**：异步落库时，MySQL的写入TPS能到多少？有没有出现死锁或行锁等待？

---

### 第七轮：异常与边界场景

**面试官**：请你思考并回答以下场景，系统会如何表现：

1. **Redis主从切换**：秒杀过程中，Redis主节点宕机，从节点升主但可能丢失了部分库存预扣数据（异步复制）。你的Lua脚本是在主节点执行的，切主后，新主节点没有那些用户的抢购成功标识，用户再次请求会怎样？会不会导致超卖？
2. **Kafka broker宕机**：Producer发送消息时，Leader副本不可用，你的配置能自动重试到新的Leader吗？如果重试超过阈值，消息发送失败，用户前端显示“抢购成功”，但后台没收到消息，你怎么补偿？
3. **用户恶意取消支付**：用户抢到后，主动取消订单（而不是超时未支付），此时是否需要恢复库存和资格？你的系统支持吗？与自动关单的逻辑是否复用？

---

### 第八轮：技术深度拷打

**面试官**：你提到Redisson延迟队列、布隆过滤器、Kafka等，但很多实习项目只是“会用”。我想听你解释：

1. **Redisson的RDelayedQueue**：它基于Redis的ZSet和List实现，请你描述当`offer`一个元素时，底层执行了哪些Redis命令？当元素到期移出时，是客户端主动轮询还是Redis有回调？
2. **布隆过滤器**：你用的是Redisson的`RBloomFilter`吗？它的误判率设置的是多少？如果误判将合法商品ID过滤掉，导致用户无法抢购，你如何解决？
3. **Kafka的顺序消费**：同一个秒杀活动的多个成功消息，是否需要保持顺序？如果需要，你在Producer端是如何设置Key的？Consumer端是单线程还是多线程？

---

最后，给你一个**场景设计题**（面试常见压轴）：

**面试官**：假设秒杀活动开始前30秒，突然有100万机器人请求刷你的秒杀接口（不同用户ID，但都是伪造的）。你的系统架构（网关层、Redis层、业务层）会怎样应对？请从限流、防刷、缓存、DB四个层面说明你现有的机制和缺失的机制。

---

好了，以上是我对你的第一个项目的拷打。请逐一回答，不要跳过任何一个追问。如果你能把这些都讲清楚，我会认为你对这个项目有真正的掌控力。如果你需要时间思考，可以逐条回复，我会继续追问。