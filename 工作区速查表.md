# LivPick 工作区速查表

这 4 个工作区用于隔离 4 套不同阶段的秒杀架构，后续应固定按目录工作，不再通过同一目录里的 `git switch` / `git checkout` 来回切主架构分支。

## 1. 目录 - 分支 - 职责

| 目录 | 绑定分支 | 主要职责 | 说明 |
| --- | --- | --- | --- |
| `C:\Users\heyunhui\IdeaProjects\LivPick` | `feature/seckill-db-only` | 纯 MySQL 秒杀实现、压测、报告 | 作为 `db-only` 专属工作区使用 |
| `C:\Users\heyunhui\IdeaProjects\LivPick-main` | `main` | 原始 `MySQL + Redis` 实现 | 作为未优化基线对照 |
| `C:\Users\heyunhui\IdeaProjects\LivPick-db-cache` | `feature/seckill-db-cache` | 优化版 `MySQL + Redis` 实现、压测、报告 | 用于 `db-cache` 架构对比 |
| `C:\Users\heyunhui\IdeaProjects\LivPick-db-cache-mq` | `feature/seckill-db-cache-mq` | `MySQL + Redis + Kafka` 实现、压测、报告 | 用于 `db-cache-mq` 架构对比 |

## 2. 日常使用方式

以后切换主架构，不再优先执行：

```powershell
git switch <branch>
```

而是直接进入对应目录：

```powershell
cd C:\Users\heyunhui\IdeaProjects\LivPick
cd C:\Users\heyunhui\IdeaProjects\LivPick-main
cd C:\Users\heyunhui\IdeaProjects\LivPick-db-cache
cd C:\Users\heyunhui\IdeaProjects\LivPick-db-cache-mq
```

建议规则：

- `LivPick` 只做 `db-only`
- `LivPick-main` 只做 `main`
- `LivPick-db-cache` 只做 `db-cache`
- `LivPick-db-cache-mq` 只做 `db-cache-mq`

## 3. 提交与推送命令

### 3.1 纯 MySQL

```powershell
cd C:\Users\heyunhui\IdeaProjects\LivPick
git add .
git commit -m "your message"
git push origin feature/seckill-db-only
```

### 3.2 原始 MySQL + Redis

```powershell
cd C:\Users\heyunhui\IdeaProjects\LivPick-main
git add .
git commit -m "your message"
git push origin main
```

### 3.3 优化版 MySQL + Redis

```powershell
cd C:\Users\heyunhui\IdeaProjects\LivPick-db-cache
git add .
git commit -m "your message"
git push origin feature/seckill-db-cache
```

### 3.4 MySQL + Redis + Kafka

```powershell
cd C:\Users\heyunhui\IdeaProjects\LivPick-db-cache-mq
git add .
git commit -m "your message"
git push origin feature/seckill-db-cache-mq
```

## 4. 首次推送新分支

如果某个分支还没有建立远程跟踪关系，首次推送用：

```powershell
git push -u origin <branch>
```

例如：

```powershell
git push -u origin feature/seckill-db-cache-mq
```

后续再推送可直接使用：

```powershell
git push
```

## 5. 什么时候还可以用 `git switch`

对这 4 条主架构分支，不建议再互相切换。

只有下面两类情况可以在对应工作区里用：

1. 从当前分支临时拉一个小分支修 bug
2. 做当前架构内的临时实验分支

例如：

```powershell
git switch -c fix/report-typo
```

不建议做的事：

```powershell
git switch main
git switch feature/seckill-db-cache
git switch feature/seckill-db-only
```

尤其不要在这 4 个工作区里来回切另外 3 条主架构分支，否则很容易再次污染 `target`、README、压测资产和配置文件。

## 6. 压测与运行隔离建议

除了代码目录隔离，运行环境也要尽量隔离：

- MySQL：不同架构使用不同库名
- Redis：不同架构使用不同端口 / 容器名 / 数据卷
- Kafka：不同架构使用独立容器或至少独立 topic 约定
- `target/benchmark`：各工作区各自产生，不混用

## 7. 快速检查命令

查看当前目录绑定的分支：

```powershell
git branch --show-current
```

查看当前工作区是否干净：

```powershell
git status --short --branch
```

查看所有 worktree：

```powershell
git worktree list
```

## 8. 一句话规则

主架构切换靠“切目录”，不是靠“切分支”。
