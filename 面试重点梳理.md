# 面试重点梳理

## 1. 整体项目升级思路

这一轮优化的核心，是把原来偏学习型的高并发项目进一步升级成更接近生产实践的架构，重点围绕 4 个问题展开：

1. 秒杀请求高并发下数据库压力大，主链路容易阻塞
2. 更新数据库后删除缓存失败，容易出现缓存和数据库不一致
3. 用户下单后长时间未支付，会占用库存
4. 不存在的数据被反复请求，会造成缓存穿透

所以整体上我做了 4 个升级：

- 用 Kafka 替换 Redis Stream，完成秒杀异步落库
- 用 Kafka 增加缓存删除补偿重试链路
- 用 Redisson 延迟队列做超时关单和库存回流
- 用 Redisson Bloom Filter + 缓存空值解决缓存穿透

---

## 2. Kafka 异步落库

### 2.1 解决的问题

Kafka 异步落库主要解决 3 个问题：

- 高并发秒杀请求直接打数据库，热点库存行竞争严重
- 同步下单链路太长，请求线程容易阻塞
- 大量无效请求不应该进入数据库

### 2.2 实现流程

完整流程是：

1. 用户发起秒杀请求
2. 服务端生成订单 ID
3. 执行 `seckill.lua`
4. Lua 在 Redis 中原子完成：
   - 判断库存是否充足
   - 判断用户是否已经下单
   - 预扣 Redis 库存
   - 记录用户下单资格
5. 如果 Lua 返回成功，Java 构造 `SeckillOrderMessage`
6. 发送 Kafka 秒杀下单消息
7. Kafka Consumer 异步消费消息
8. 消费端按 `userId` 加 Redisson 分布式锁
9. 查询数据库，判断用户是否已经对该优惠券下过单
10. 数据库执行 `stock = stock - 1 and stock > 0`
11. 创建订单记录
12. 创建成功后投递超时关单延迟消息

### 2.3 为什么这样设计

#### 为什么 Redis 只做资格校验，不直接落库

因为秒杀场景的第一目标是把高并发流量挡在数据库外面。Redis 在内存里做库存和一人一单校验，响应更快，也能显著减少无效请求对数据库的冲击。

#### 为什么还要异步落库

如果请求线程同步完成库存扣减、查重和订单写库，链路会非常长，一旦秒杀流量冲上来，线程池、连接池和数据库都会承压。改成 Kafka 异步后，请求线程只做资格校验和消息投递，主链路明显更轻。

#### 为什么消费端还要二次校验

因为消息队列语义更接近至少一次投递，理论上可能重复消费；另外分布式场景下不能只依赖入口校验，最终落库前必须再做数据库侧校验和兜底。

#### 为什么消费端还要加 Redisson 锁

消费端加锁不是为了替代数据库，而是为了降低同一用户重复消息并发处理时的竞争，让查重和创建订单逻辑更加稳定。

### 2.4 关键细节

#### Kafka 发送失败怎么办

发送 Kafka 失败时，会执行 Redis 回滚脚本，把已经占用的 Redis 库存和用户资格恢复回来，避免出现“Redis 扣减成功但消息没发出去”的脏状态。

#### 如何防止超卖

我做了三层保护：

1. Redis Lua 原子校验和预扣库存
2. 消费端按用户维度加 Redisson 锁
3. 数据库层用 `stock > 0` 条件扣减，并做订单查重

#### 如何保证一人一单

同样是多层控制：

1. Redis 集合记录用户是否已抢过
2. Kafka 消费端按用户加锁
3. 数据库落库前再次查重

### 2.5 面试时的一句话总结

我把秒杀链路改造成“Redis 做入口原子校验，Kafka 做异步削峰，MySQL 做最终落库”的结构，核心目标是降低数据库热点竞争，同时兼顾库存正确性和一人一单约束。

---

## 3. Kafka 缓存删除补偿重试

### 3.1 解决的问题

这块主要解决的是缓存一致性问题：如果数据库更新成功，但缓存删除失败，就可能出现用户读到旧数据的情况。

### 3.2 实现流程

完整流程是：

1. 业务更新数据库
2. 数据库更新成功后，尝试删除对应缓存
3. 如果删除成功，流程结束
4. 如果删除失败，构造 `CacheDeleteRetryMessage`
5. 发送 Kafka 补偿消息
6. Kafka Consumer 异步消费消息，再次执行删缓存
7. 如果仍然失败，递增重试次数并重新发送补偿消息
8. 超过最大重试次数后进入错误日志和告警处理

### 3.3 为什么这样设计

#### 为什么采用旁路缓存模式

因为旁路缓存模式实现简单、业务侵入小，也比较符合读多写少场景。更新数据库后删缓存，而不是先更新缓存，可以避免业务层双写缓存带来的复杂度。

#### 为什么还要补偿重试

因为“更新 DB 后删缓存”本身不是绝对可靠的，删除缓存这一步也可能因为网络抖动、Redis 异常等原因失败，所以要有消息补偿机制做兜底。

#### 为什么不用延迟双删作为主方案

延迟双删对时机比较敏感，只能缓解部分并发窗口问题，不能彻底保证一致性；而“删缓存失败后消息补偿重试”更容易继续扩展成统一的补偿链路。

### 3.4 关键细节

#### TTL 在这里的作用

TTL 不是主方案，而是最后一道兜底。即使补偿链路异常，脏缓存也不会永久存在。

#### 为什么用 Kafka 做补偿

因为 Kafka 更适合承接异步重试任务，补偿逻辑和业务主链路解耦，也更方便后续扩展重试、死信和监控能力。

### 3.5 面试时的一句话总结

我在旁路缓存基础上又补了一层 Kafka 补偿重试，把“更新数据库后删缓存失败”这个异常场景也兜住了，降低缓存和数据库不一致的风险。

---

## 4. Redisson 延迟队列超时关单与库存回流

### 4.1 解决的问题

这块主要解决两个问题：

- 用户下单后不支付，库存被无效占用
- 如果不自动关单，秒杀库存无法及时释放

### 4.2 实现流程

完整流程是：

1. Kafka Consumer 创建订单成功
2. 构造 `OrderTimeoutMessage`
3. 投递到 Redisson 延迟队列
4. 到达超时时间后，后台线程消费延迟消息
5. 执行 `closeTimeoutOrder(orderId)`
6. 查询订单当前状态
7. 通过 `where status = 1` 的条件更新，把订单从“未支付”改成“已取消”
8. 如果关单成功，则回补数据库库存
9. 同时回补 Redis 中的库存和用户下单资格
10. 如果延迟队列出现异常，定时任务会扫描超时未支付订单并再次执行关单

### 4.3 为什么这样设计

#### 为什么用延迟队列而不是单纯扫表

单纯定时扫表虽然简单，但实时性一般，而且数据量大了以后对数据库压力明显。延迟队列可以在订单创建时就把超时处理预约好，时效性更好。

#### 为什么还要保留定时任务兜底

因为任何异步链路都可能出现消费者异常、服务重启或消息遗漏的情况，所以还要用一个低频定时扫描任务兜底，防止订单长期不关闭。

#### 为什么关单要用状态条件更新

因为支付和关单可能并发发生。如果不带状态条件，可能出现“用户已经支付成功，但后台仍把订单关掉”的问题。用：

```sql
where id = ? and status = 1
```

就能把支付和关单变成对同一状态条件的竞争更新，本质上就是基于状态条件的 CAS。

### 4.4 关键细节

#### 为什么要同时回补 DB 和 Redis

因为当前架构里：

- Redis 承担入口资格和预扣库存
- MySQL 承担最终库存事实

所以关单后必须把这两层都恢复回来，才能保证库存视图最终一致。

#### 支付和关单并发时怎么保证正确性

支付只能把 `status=1` 更新成 `status=2`，关单只能把 `status=1` 更新成 `status=4`。谁先更新成功，谁就拿到状态变更权；另一个操作更新行数为 0，就说明订单状态已经被改过，不再继续执行。

### 4.5 面试时的一句话总结

我用 Redisson 延迟队列把超时关单从“被动处理”变成了“主动调度”，再加一个定时扫描兜底，同时用状态条件更新解决支付和关单的并发冲突。

---

## 5. Redisson Bloom Filter + 缓存空值

### 5.1 解决的问题

这块主要解决缓存穿透问题，也就是大量不存在的数据请求反复穿透到数据库。

### 5.2 实现流程

完整流程是：

1. 应用启动时初始化 `RBloomFilter`
2. 将已有店铺 ID 装载到布隆过滤器中
3. 用户查询店铺详情时，先查布隆过滤器
4. 如果布隆过滤器判断“不存在”，直接返回，不访问 Redis 和数据库
5. 如果布隆过滤器判断“可能存在”，继续走缓存查询
6. Redis 命中则直接返回
7. Redis 未命中则查询数据库
8. 数据存在则回写缓存
9. 数据不存在则写入空值缓存

### 5.3 为什么这样设计

#### 为什么不用单一方案

单独用布隆过滤器有误判风险，只能拦住绝大部分无效请求；  
单独用缓存空值虽然能挡住重复穿透，但第一次无效请求仍然会打到数据库。  
把两者组合起来，才能兼顾拦截效率和实现成本。

#### 布隆过滤器的优势

- 查询快
- 内存占用低
- 很适合用来预过滤大量不存在的 ID 请求

#### 布隆过滤器的局限

- 存在误判
- 删除元素不方便
- 参数设置不合理会导致误判率上升

所以它更适合读多写少、可接受极低误判率的查询场景。

### 5.4 关键细节

#### 为什么还要缓存空值

因为布隆过滤器并不是百分之百精准，少量误判请求仍可能打到数据库，这部分请求需要由缓存空值继续兜底，避免同一无效请求反复穿透。

#### 为什么布隆过滤器适合这个场景

店铺 ID 集合相对稳定，查询频率高，且缓存穿透一旦发生会明显增加数据库压力，所以非常适合作为布隆过滤器的第一批接入场景。

### 5.5 面试时的一句话总结

我把缓存穿透防护改成“布隆过滤器预判 + 缓存空值兜底”的组合方案，先把绝大多数无效请求挡在入口，再用空值缓存防止漏网请求重复打库。

---

## 6. 面试时建议的整体表达顺序

如果面试官让你整体讲这一轮优化，可以按这个顺序回答：

1. 先讲业务痛点：高并发秒杀、缓存一致性、未支付订单占库存、缓存穿透
2. 再讲架构升级：Redis 做入口校验，Kafka 做异步削峰和补偿，Redisson 做延迟调度和布隆过滤器，MySQL 做最终事实存储
3. 再展开 4 个重点模块：
   - Kafka 异步落库
   - Kafka 缓存补偿重试
   - Redisson 延迟队列超时关单
   - Bloom Filter + 缓存空值防穿透
4. 最后总结你做的核心取舍：
   - 高并发请求尽量挡在数据库外
   - 异步链路尽量做补偿
   - 并发状态流转尽量用 CAS 条件更新
   - 缓存问题分场景分别治理

---

## 7. 面试时的高频追问与回答方向

### 7.1 Kafka 异步落库为什么不是强一致

因为下单资格先在 Redis 中完成，订单最终由 Kafka 异步落库，所以这套方案本质上是最终一致性，而不是强一致性。但它能更好地平衡吞吐、响应时间和系统可用性。

### 7.2 为什么不直接用数据库做秒杀

数据库可以保证正确性，但高并发下热点库存行竞争严重，吞吐很快触顶。Redis 更适合做高并发入口校验，Kafka 更适合做异步削峰。

### 7.3 为什么支付与关单不用悲观锁

因为支付和关单本质上只是对单条订单状态做有限状态流转，用状态条件更新就能解决问题，没有必要引入更重的串行化锁。

### 7.4 布隆过滤器误判怎么办

误判意味着“本来不存在的数据被判断成可能存在”，它不会影响正确性，只会让少量请求继续走后续缓存和数据库查询；再由缓存空值做兜底即可。
